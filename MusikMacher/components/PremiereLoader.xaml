<UserControl x:Class="MusikMacher.components.PremiereLoader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MusikMacher.components"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
  <Grid Margin="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <ui:TextBox Grid.Row="0" Text="{Binding PremiereFilePath}" />
    <Button Grid.Row="1" Command="{Binding LoadPremiereDataCommand}">Load Premiere</Button>
    <TextBlock Grid.Row="2" Text="{Binding PremiereLog}" />
  </Grid>
</UserControl>